 ```git
>git remote add origin  https://github.com/jojotaro123/Django_study.git
上面写自己仓库地址,关联远程仓库

git pull origin master --allow-unrelated-histories
第二个是 将远程仓库的代码 同步到本地做一个合并

git push origin master

 ```

